export const TIME_FORMATS = ["h:mm A", "hh:mm A", "HH:mm"]

// t('timestampFormats.absolute')
// t('timestampFormats.relative')
// t('timestampFormats.both')
export const TIMESTAMP_FORMATS = ["absolute", "relative", "both"]
