const initialState = {
  currentBusiness: {},
  currentUser: {},
  userBusinesses: [],
}

export default function appState(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
